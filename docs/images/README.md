# LiteSnap screenshots

Drop real product screenshots here and keep these filenames so the root `README.md` gallery stays valid:

| File | Suggested capture |
|---|---|
| `capture.png` | Region selection overlay with indigo border + resize handles |
| `annotate.png` | Annotation toolbar + shapes / text / emoji on a capture |
| `scroll-capture.png` | Scroll capture live preview panel while stitching |
| `pin.png` | Floating pinned screenshot window on the desktop |
| `icon.png` | App icon (copied from `app/build/icon.png`) |

Tips:
- Prefer 16:9 or ~1600×900 PNG.
- Hide personal data before capturing.
- Replace the current placeholder mockups with real LiteSnap UI shots when ready.
